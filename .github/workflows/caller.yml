name: Caller

# Controls when the workflow will run
on:
  workflow_dispatch:

jobs:
  call-workflow-passing-data:
    uses: devops-max/simple-java-maven-app/.github/workflows/reusable.yml@master
    with:
      username: Harry
